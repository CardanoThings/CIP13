/**
 * Public site hostname for GitHub Pages (custom domain).
 * When the domain changes, update:
 * 1. SITE_HOST below
 * 2. static/CNAME (must match SITE_HOST)
 * 3. DNS at your registrar
 */
export const SITE_HOST = 'cip13.cardanothings.io';
export const SITE_ORIGIN = `https://${SITE_HOST}`;
